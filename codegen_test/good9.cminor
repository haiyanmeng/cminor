//for loop

main:function integer() = {
	i:integer;
	s:integer = 0;
	for(i=1; i<5 && i > 0 && s <20; i++) {
		s = s + i;
	}
	print s, '\n'; //10

	s = 0;
	for(i=1; i==1 && i<5; i++) {
		s = s + i;
	}
	
	print s, '\n'; //1


/* infinite loop
	//for(i=1; ; i++) {
	//for(i=1; ;){
	for(; ;){
		s = s + i;
	}
	print s, '\n'; //1
*/
	return 0;
}
