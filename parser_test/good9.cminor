/* declaration */

x:integer = 123243535; // declaration integer with initialization

x1:string = "abc"; // declaration string with initialization

y:char = 'q'; // declaration char with initialization

c1: array[5] integer={1,2,3,4,5}; // declaration 1-D array with initialization


/* function prototype */
puts: function void ( s: string ) = {
	print s;
}

/* function definition */
main: function integer ();

main: function integer () = {

	puts("hello");
x:integer  = 2;
a:string = "a";
b:string = "b";
/* if statement */
if (x == 5)
	print a;
else
	print b;

x = 5;
if (x == 5) {
	print a;
} else {
	print b;
}

if (x == 5)
	print a;
	print b;

/* compound statement */
{x = 5; (x=1); {x = 1;} }

/* c comment */

/* arithmetic statement */
a1:integer = 1;
c:array[3] integer = {1,2,3};
b1:integer = c[1];
b2:integer = c[1^2+1*a1];
boo:integer = (b1==b2);
b3: integer = b1 < 6 || c[1] >= 7 && c[0] > 6 || c[1] <= 7;
aa:integer = -2;
aa:integer = 5 + -2 - c[1] * 12 / 20 % 21 + c[1]++ + aa--;
aaa: integer = !(aa + 3);
a4:integer = !(-aa);

for (a4=1; a4< 2; a4++)
	print a4;

for (a4=1; a4< 2; a4++) {
	{
		a4=a4^3;
	}
}
print a4;

return 0;
}






