main: function integer (x:char,y:boolean) = {
	a: array[5+x-323] integer={1,2,3,4,5}; // declaration 1-D array with initialization
	f(a*b,a+1,b,'\n', '\0', '\r', '\t', '\\', '\b', c);
	f(a*b,a+-1,b,'\b',c);
	f(a+b,a+ 1,b,'d',c);
}

	
